# RallyGuard

RallyGuard — веб-прототип для BioTech-трека Overclock Hackathon 2026. Теннисист-любитель отмечает сон, энергию, усталость, стресс и дискомфорт, а приложение показывает личную динамику и отдельное объяснение каждого сигнала.

Полная концепция стартапа, сценарии пользователей, отличие от похожих продуктов и план развития описаны в [docs/STARTUP_CONCEPT.md](docs/STARTUP_CONCEPT.md).

## Личный MVP

1. Зарегистрируйтесь по email и паролю на https://rallyguard.vercel.app/, выбрав роль игрока.
2. Заполните первый опрос: новый аккаунт начинает с пустой истории.
3. Получите объяснение сигналов и добавьте занятие.
4. Выйдите и войдите снова: ответы сохраняются в PostgreSQL Supabase.
5. В профиле можно указать теннисную цель, уровень, обычную частоту занятий и ближайший матч, скачать или удалить историю.
6. Тренер создаёт отдельный аккаунт и передаёт игроку свой код. Игрок вводит код и явно соглашается на передачу краткой сводки. Доступ можно отозвать в любой момент.

Личный режим использует Supabase Auth и таблицу `rg_personal_records`. Python проверяет
пользователя через Auth и передаёт его JWT в PostgREST. Политика RLS ограничивает чтение,
запись и удаление владельцем `auth.uid() = user_id`. Токены хранятся в HttpOnly cookies;
пароли и секретный административный ключ не сохраняются в клиентском коде.
Повторный опрос за ту же дату атомарно обновляет запись.

## Сценарий демо

1. Откройте [работающее демо](https://rallyguard.vercel.app/) и нажмите **Посмотреть пример без регистрации**.
2. Откройте «Опрос» и измените сон, энергию, стресс или усталость.
3. Сохраните ответы: обзор покажет наблюдение и следующий шаг по каждому изменившемуся показателю.
4. Добавьте тренировку: после сохранения откроется короткий опрос о качестве занятия, энергии и дискомфорте.
5. В «Закономерностях» посмотрите связку «до → занятие → после». Выводы появятся после пяти связанных занятий.
6. В «Профиле» переключите сценарии «Усталость» и «Нужен разговор».
7. В «Тренере» сравните три вымышленных профиля.

Все стартовые профили и показатели вымышлены. Каждая браузерная сессия получает собственное пространство данных. Для защиты хакатона используйте только синтетические данные.

## Почему проект подходит BioTech

Кейс требует анализировать состояние и нагрузку атлета, показывать адаптивную рекомендацию и интерфейс для спортивного штаба. В MVP это реализовано как:

- чек-ин сна, энергии, стресса, усталости, дискомфорта и опциональных HRV/пульса;
- описательный индекс состояния с объяснением факторов и качеством истории;
- нагрузка `минуты × RPE` и история занятий;
- отдельные наблюдения и действия по сну, энергии, стрессу, усталости, дискомфорту и нагрузке;
- обзор тренера с возможностью сохранить решение;
- JSON-импорт показателей, экспорт и удаление демо-сессии;
- возможность подключить Groq для текстовых объяснений после настройки серверного ключа; в опубликованном демо AI-чат пока отключён.

Индекс в прототипе не является вероятностью травмы, медицинским допуском или диагностикой. При дискомфорте, мешающем движению, интерфейс предлагает обсудить состояние со специалистом. Основной разбор рассчитывается прозрачными правилами Python, поэтому работает без внешнего AI API.

## Запуск локально

Требуется Python 3.12+.

```powershell
python -m venv .venv
.\.venv\Scripts\python.exe -m pip install -r requirements.txt
.\.venv\Scripts\python.exe run.py
```

Откройте http://127.0.0.1:8000. Для обычного запуска используется SQLite в `data/rallyguard.db`. Скопируйте `.env.example` в `.env`, если нужно изменить режим хранения.

## Supabase

Подключён проект `oitbqygljigiqrczqzdd`. Миграция личных аккаунтов:
`supabase/migrations/202609230001_personal_accounts.sql` (уже применена в этом проекте).
Публичные URL и publishable key заданы в `backend/accounts.py`. Это публичный ключ
приложения; права обеспечиваются JWT пользователя и RLS. Для другого проекта задайте
`SUPABASE_AUTH_URL` и `SUPABASE_PUBLISHABLE_KEY`, примените миграцию.

В Authentication включите Email/password. Для подтверждений и восстановления пароля
нужен собственный SMTP; стандартная отправка Supabase ограничена. Site URL должен быть
`https://rallyguard.vercel.app`. Обработка подтверждения email реализована.
Для текущего MVP по решению владельца проекта подтверждение email временно отключено:
регистрация сразу открывает аккаунт, владение почтовым адресом не проверяется.
Перед публичным запуском за пределами хакатона подключить SMTP и вернуть подтверждение.
Интерфейс восстановления пароля готов, но отправка ссылки выключена до настройки SMTP.
После подключения SMTP включите подтверждение email в Supabase и установите
`EMAIL_DELIVERY_ENABLED=1` в Vercel. Проверьте шаблоны confirmation и recovery,
а также разрешённый redirect `https://rallyguard.vercel.app/`.

Миграция ролей и согласия: `supabase/migrations/202609230002_roles_and_coaches.sql`.
Она добавляет неизменяемую роль аккаунта, теннисный профиль, связь игрока с тренером,
ограниченный отчёт и привязку Telegram. Миграция применена в SQL Editor проекта
23 сентября 2026 года. Старые аккаунты после миграции стали игроками.

`DATABASE_MODE` относится **только к старой демо-песочнице**; его можно оставить `sqlite`.
Личная история всегда идёт в Supabase, без fallback на временный диск Vercel.
Для постоянной демо-песочницы примените `202609220001_rallyguard.sql` и задайте
`DATABASE_MODE=supabase`, `SUPABASE_URL`, `SUPABASE_SERVICE_ROLE_KEY` только на сервере.

## AI

Совет под индексом рассчитывается на сервере через Gemini, если задан `GEMINI_API_KEY`.
Ключ хранится только в серверной переменной Vercel и в локальном игнорируемом `.env`.
Игрок сначала нажимает «Получить совет ИИ» и видит, какие данные будут переданы.
Python учитывает всю личную историю, а Gemini получает компактные итоги, последние
7 опросов и 5 тренировок. Ответ ограничен двумя предложениями и кэшируется в текущей
вкладке на 30 минут. Если API недоступен, показывается совет рассчитанного алгоритма.
Текущий индекс и личная базовая линия не рассчитываются моделью.

## Telegram для тренера

Создайте бота через BotFather. В переменные Vercel добавьте `TELEGRAM_BOT_TOKEN`,
`TELEGRAM_BOT_USERNAME` (без `@`), случайный `TELEGRAM_WEBHOOK_SECRET` и
`SUPABASE_SERVICE_ROLE_KEY` **только на сервере**. Не помещайте их в Git.
После деплоя зарегистрируйте webhook скриптом `scripts/register_telegram_webhook.py`:
локально добавьте токен и webhook-secret в неотслеживаемый `.env`, затем запустите
`python scripts/register_telegram_webhook.py`. В профиле тренера появится ссылка
привязки на 10 минут. Команды бота: `/players`, `/report 1`, `/unlink`.
Отчёты доступны только привязанному тренеру и только пока игрок не отозвал согласие.
Личные заметки, пульс и HRV в сообщения не входят.

## Тесты

```powershell
.\.venv\Scripts\python.exe -m pytest -q
```

Проверки покрывают API, валидацию, изоляцию сессий, персональные сигналы, удаление/экспорт и статические файлы.

## Деплой

Проект Vercel: [`rallyguard`](https://vercel.com/n0nam3e01s-projects/rallyguard).
Project ID: `prj_bXmVY8KtUvGQTQUcmu0cDZD8uAqs`. Репозиторий: `n0nam3e01/equa.ai`.
Изменения в `main` запускают автоматическую публикацию.

Репозиторий рассчитан на Vercel FastAPI: корень проекта — `.`, Output Directory оставьте пустым. `api/index.py` экспортирует FastAPI-приложение, а `vercel.json` направляет запросы к нему. Для постоянной истории на Vercel используйте Supabase; SQLite там временный.

Перед отправкой проекта на AI Startify команда должна добавить публичную ссылку на GitHub, live-ссылку или видео до 3 минут, питч до 12 слайдов и опубликовать карточку после AI Incubator с промокодом `OVRC`.

## Структура

| Путь | Назначение |
|---|---|
| `frontend/` | Адаптивная тёмная спортивная вёрстка и клиентский сценарий |
| `backend/accounts.py` | Вход, регистрация, обновление сессии и личная история через RLS |
| `backend/telegram_bot.py` | Проверяемый webhook, привязка тренера и краткие отчёты |
| `backend/main.py` | FastAPI-маршруты, cookie-сессия и границы доступа |
| `backend/analytics.py` | Объяснимый описательный расчёт состояния |
| `backend/content.py` | Старый каталог уроков, не используемый в основном интерфейсе |
| `backend/storage.py` | SQLite/Supabase адаптер |
| `sql/` | Локальная SQL-схема |
| `supabase/migrations/` | Схема PostgreSQL для проекта Supabase |
| `tests/` | Серверные проверки |

Дизайн: переключаемые графитовая и светлая темы, сдержанные круговые шкалы самооценки,
Manrope, SVG-иконки и адаптивный layout для телефона и компьютера. Шкалы построены по ответам,
а не по автоматическим измерениям носимого устройства.
