# RallyGuard

RallyGuard — веб-прототип для BioTech-трека Overclock Hackathon 2026. Он помогает теннисисту-любителю коротко отметить самочувствие, выбрать следующий учебный шаг и замечать изменения нагрузки без медицинских обещаний.

## Сценарий демо

1. Откройте сайт и нажмите **Попробовать демо**.
2. На экране «Сегодня» выберите чек-ин, затем откройте предложение или урок.
3. Пройдите квиз «После ошибки» или «Правила подачи».
4. В «Моя динамика» добавьте тренировку и посмотрите объяснение факторов.
5. В «Профиль» переключите демонстрационный сценарий «Усталость» или «Нужен разговор».
6. В «Обзор тренера» выберите игрока и сохраните предложение по изменению плана.

Все стартовые профили и показатели вымышлены. Каждая браузерная сессия получает собственное пространство данных. Для защиты хакатона используйте только синтетические данные.

## Почему проект подходит BioTech

Кейс требует анализировать состояние и нагрузку атлета, показывать адаптивную рекомендацию и интерфейс для спортивного штаба. В MVP это реализовано как:

- чек-ин сна, энергии, стресса, усталости, дискомфорта и опциональных HRV/пульса;
- описательный индекс состояния с объяснением факторов и качеством истории;
- нагрузка `минуты × RPE` и история занятий;
- предложение следующего шага с учётом состояния;
- уроки по концентрации, восстановлению и правилам тенниса;
- обзор тренера с возможностью сохранить решение;
- JSON-импорт показателей, экспорт и удаление демо-сессии;
- AI-чат через Groq, если ключ добавлен на сервере.

Индекс в прототипе не является вероятностью травмы, медицинским допуском или диагностикой. При дискомфорте, мешающем движению, интерфейс предлагает обсудить состояние со специалистом. AI извлекает и объясняет текст, но не принимает медицинское решение.

## Запуск локально

Требуется Python 3.12+.

```powershell
python -m venv .venv
.\.venv\Scripts\python.exe -m pip install -r requirements.txt
.\.venv\Scripts\python.exe run.py
```

Откройте http://127.0.0.1:8000. Для обычного запуска используется SQLite в `data/rallyguard.db`. Скопируйте `.env.example` в `.env`, если нужно изменить режим хранения.

## Supabase

Выполните `supabase/migrations/202609220001_rallyguard.sql` в SQL Editor проекта Supabase. Затем добавьте локально или в Vercel Environment Variables:

```text
DATABASE_MODE=supabase
SUPABASE_URL=https://oitbqygljigiqrczqzdd.supabase.co
SUPABASE_SERVICE_ROLE_KEY=...
```

Ключ `service_role` хранится только на сервере. Браузер обращается только к нашему API. RLS включён, публичным ролям доступ к таблицам запрещён.

## AI

Для чата можно добавить `GROQ_API_KEY` и, при необходимости, `GROQ_MODEL` в `.env`. По умолчанию используется `openai/gpt-oss-20b`. При отсутствии ключа сайт продолжает работать: уроки, формы и описательная аналитика не зависят от AI. Не отправляйте в бесплатный внешний API реальные медицинские сведения.

## Тесты

```powershell
.\.venv\Scripts\python.exe -m pytest -q
```

Проверки покрывают API, валидацию, изоляцию сессий, квизы, расчёт состояний, удаление/экспорт и статические файлы.

## Деплой

Репозиторий рассчитан на Vercel FastAPI: корень проекта — `.`, Framework Preset — **FastAPI**, Output Directory оставьте пустым. `vercel.json` и `pyproject.toml` уже содержат точку входа `backend.main:app`. Для постоянной истории на Vercel используйте Supabase; SQLite там временный.

Перед отправкой проекта на AI Startify команда должна добавить публичную ссылку на GitHub, live-ссылку или видео до 3 минут, питч до 12 слайдов и опубликовать карточку после AI Incubator с промокодом `OVRC`.

## Структура

| Путь | Назначение |
|---|---|
| `frontend/` | Адаптивная светлая спортивная вёрстка и клиентский сценарий |
| `backend/main.py` | FastAPI-маршруты, cookie-сессия и границы доступа |
| `backend/analytics.py` | Объяснимый описательный расчёт состояния |
| `backend/content.py` | Уроки, квизы и ссылки на источники |
| `backend/storage.py` | SQLite/Supabase адаптер |
| `sql/` | Локальная SQL-схема |
| `supabase/migrations/` | Схема PostgreSQL для проекта Supabase |
| `tests/` | Серверные проверки |

Дизайн: молочный фон, лесной зелёный, теннисный лайм, Manrope, SVG-иконки и responsive layout для телефона и компьютера.
