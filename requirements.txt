# Python API и сервер, который одновременно отдаёт интерфейс браузеру.
fastapi==0.141.1
uvicorn==0.53.0
python-dotenv==1.2.3
# Обучение объяснимой модели на табличных синтетических данных.
numpy==2.3.5
scikit-learn==1.9.1
# HTTP-клиент используется только сервером для Supabase REST API.
httpx==0.28.1
# Проверки API, SQL-хранилища и расчётов потерь.
pytest==9.1.1
